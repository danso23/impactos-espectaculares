---
deployment:
  tasks:
    - /bin/sh -c 'set -x; LOG=/home2/enticonfiamos/public_html/deploy_full.log; echo "=== DEPLOY START ===" >> "$LOG" 2>&1; /bin/date >> "$LOG" 2>&1; /bin/mkdir -p /home2/enticonfiamos/public_html/espectacularesapi >> "$LOG" 2>&1; /bin/mkdir -p /home2/enticonfiamos/app.enticonfiamos.com.mx >> "$LOG" 2>&1; /bin/cp -R espectacularesapi/* /home2/enticonfiamos/public_html/espectacularesapi >> "$LOG" 2>&1; /bin/ls -1 /home2/enticonfiamos/public_html/espectacularesapi/database/migrations | /usr/bin/tail -n 5 >> "$LOG" 2>&1; /usr/local/bin/php /home2/enticonfiamos/public_html/espectacularesapi/artisan migrate --force >> "$LOG" 2>&1; echo "=== DEPLOY END ===" >> "$LOG" 2>&1'
